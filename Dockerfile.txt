# Use a imagem full oficial do n8n
FROM n8nio/n8n:latest

# Instala bibliotecas adicionais que são exigidas por alguns nós (ex: Spreadsheet File, HTML Extract, etc.)
USER root

RUN apt-get update && apt-get install -y \
  python3 \
  python3-pip \
  build-essential \
  libxss1 \
  libappindicator1 \
  libindicator7 \
  libnss3 \
  libatk-bridge2.0-0 \
  libgtk-3-0 \
  fonts-liberation \
  libasound2 \
  libatk1.0-0 \
  libcups2 \
  libdbus-1-3 \
  libx11-xcb1 \
  libxcomposite1 \
  libxdamage1 \
  libxrandr2 \
  xdg-utils \
  libgbm-dev \
  wget \
  curl \
  unzip \
  && apt-get clean

# Reinstala dependências extras, se quiser
RUN npm install --legacy-peer-deps --omit=dev

# Volta para o usuário padrão do n8n
USER node
